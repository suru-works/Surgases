import React from 'react';
import PropTypes from 'prop-types';

const SalesComponent = () => {
    return (
        <div>
            <h3>Tengo poder pero no tanto :v</h3>
        </div>
    );
};

SalesComponent.propTypes = {};

export default SalesComponent;